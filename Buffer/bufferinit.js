// buffer 再console中使用十六进制进行展示
console.log(Buffer.alloc(10))
console.log(Buffer.alloc(20))
console.log(Buffer.alloc(6,1))
console.log(Buffer.allocUnsafe(10))
console.log(Buffer.from([1,2,4]));
console.log(Buffer.from('test'));

